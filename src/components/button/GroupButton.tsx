import React, { Component } from 'react'
import { Button } from 'antd'


interface ButtonProps {
  type: string;
  size: string;
}

export default class GroupButton({type, size}: ButtonProps) {
    return (
        <Button
    )
}
